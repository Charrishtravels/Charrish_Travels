---
import { SITE } from '../consts';
---

<footer class="bg-emerald-800 text-white">
  <div class="mx-auto grid max-w-5xl gap-8 px-4 py-12 sm:grid-cols-3 sm:px-6">
    <div>
      <p class="font-serif text-lg font-semibold">{SITE.name}</p>
      <p class="mt-2 text-sm text-white/70">{SITE.tagline}</p>
    </div>

    <div class="text-sm text-white/80">
      <p class="font-semibold text-white">Contact</p>
      <ul class="mt-2 space-y-1.5">
        <li><a href={`tel:${SITE.phone}`} class="hover:text-white">{SITE.phone}</a></li>
        <li><a href={`mailto:${SITE.email}`} class="hover:text-white">{SITE.email}</a></li>
        <li>
          <a href={`https://wa.me/${SITE.whatsapp}`} class="hover:text-white" target="_blank" rel="noopener">
            WhatsApp us
          </a>
        </li>
        <li>{SITE.location}</li>
      </ul>
    </div>

    <div class="text-sm text-white/80">
      <p class="font-semibold text-white">Follow</p>
      <ul class="mt-2 space-y-1.5">
        <li><a href={SITE.instagram} class="hover:text-white" target="_blank" rel="noopener">Instagram</a></li>
        <li><a href={SITE.facebook} class="hover:text-white" target="_blank" rel="noopener">Facebook</a></li>
      </ul>
    </div>
  </div>

  <div class="border-t border-white/15 px-4 py-4 text-center text-xs text-white/60 sm:px-6">
    &copy; {new Date().getFullYear()} {SITE.name}. All rights reserved.
  </div>
</footer>
